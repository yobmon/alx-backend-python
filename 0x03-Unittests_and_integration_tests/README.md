## 🧪 Testing `access_nested_map`

### Overview

This project includes unit tests for the function `access_nested_map`, which retrieves values from a nested mapping (like nested dictionaries) using a sequence of keys.

Example usage:

```python
>>> access_nested_map({"a": {"b": {"c": 1}}}, ["a", "b", "c"])
1
